# COTIS PTW 기안 자동화 구축 가이드 (Power Automate Desktop)

목표: 매일 아침 사람 개입 없이 COTIS에 위험공종 사전작업허가서(PTW) 기안을 자동 상신.

---

## 0. 준비물

| 항목 | 내용 |
|------|------|
| Power Automate Desktop (PAD) | Windows 10/11 무료 포함. 시작 메뉴에서 "Power Automate" 검색, 없으면 MS 스토어에서 설치 |
| Edge IE 모드 | IE는 지원 종료. Edge 설정 → 기본 브라우저 → "Internet Explorer 모드에서 사이트 다시 로드 허용" = 허용, COTIS 주소를 IE 모드 페이지에 등록 |
| PTW 첨부파일 | `ptw.html`로 만든 PDF (또는 COTIS 내 직접 입력이면 불필요) |
| COTIS 계정 | 아이디/비밀번호 |

> IE가 아직 실행되는 PC라면 그대로 써도 되지만, 언제 막힐지 모르니 Edge IE 모드 기준으로 만드는 것을 권장.

---

## 1. PAD 흐름(Flow) 설계

새 흐름 이름: `COTIS_PTW_상신`

### 단계 1 — 변수 준비
1. 액션 `변수 설정`:
   - `작업일자` = `%FormatDateTime(CurrentDateTime, 'yyyy-MM-dd')%`
   - `기안제목` = `위험공종 사전작업허가서(%작업일자%)`
   - `첨부경로` = `C:\PTW\PTW_%작업일자%.pdf` (첨부 방식일 때만)
2. 계정정보는 흐름에 직접 쓰지 말고 액션 `입력 대화 표시` 대신 **PAD의 "중요 텍스트(민감한 값)" 변수** 또는 Windows 자격 증명 관리자 사용 권장. (최소한 흐름 공유 금지)

### 단계 2 — 브라우저 실행 & 로그인
1. 액션 `새 Internet Explorer 시작` (Edge IE 모드도 이 액션으로 자동화 가능)
   - URL: COTIS 로그인 주소
2. `웹 페이지의 텍스트 필드 채우기` ×2: 아이디 입력칸 / 비밀번호 입력칸
   - UI 요소 지정: 액션 추가 시 "UI 요소 추가" → 실제 COTIS 화면에서 해당 칸에 마우스 올리고 Ctrl+클릭
3. `웹 페이지의 버튼 누르기`: 로그인 버튼
4. `대기` 2~3초 또는 `웹 페이지 콘텐츠 대기`: 메인 화면 로딩 확인

### 단계 3 — 기안 화면 이동
1. `웹 페이지의 링크 클릭` 또는 `버튼 누르기`로 메뉴 이동:
   - 예: 안전관리 → 위험공종 사전작업허가 → 기안 작성
   - 메뉴가 프레임/팝업이면: 팝업 창은 `창에 연결` 액션으로 새 창을 붙잡은 뒤 계속 진행

### 단계 4 — 항목 입력
고정값은 흐름에 하드코딩, 매일 바뀌는 값은 단계 1의 변수 사용.

| 입력칸 | 값 |
|--------|-----|
| 제목 | `%기안제목%` |
| 작업일자 | `%작업일자%` |
| 작업내용/위치/인원 등 | 고정값 또는 엑셀에서 읽기(아래 5. 참고) |

- 텍스트칸: `웹 페이지의 텍스트 필드 채우기`
- 콤보박스: `웹 페이지에서 드롭다운 목록 값 설정`
- 체크박스: `웹 페이지의 확인란 상태 설정`

### 단계 5 — 파일 첨부 (첨부 방식일 때)
1. `웹 페이지의 버튼 누르기`: 파일첨부/찾아보기 버튼 → Windows 파일 선택창이 뜸
2. 파일 선택창은 웹이 아니라 Windows 창이므로:
   - `창의 텍스트 필드 채우기`에 `%첨부경로%` 입력 → `창의 버튼 누르기`로 "열기" 클릭
   - 요소 인식이 안 되면 최후 수단으로 `키 보내기`: `%첨부경로%{Enter}`

### 단계 6 — 결재선 지정 & 상신
1. 결재선이 매번 같으면: 저장된 결재선 불러오기 버튼 클릭 (COTIS에 결재선 저장 기능이 있으면 미리 저장해 둘 것 — 자동화가 훨씬 단순해짐)
2. `웹 페이지의 버튼 누르기`: 상신/결재요청 버튼
3. 확인 팝업(alert)이 뜨면: IE 자동화에서는 대부분 자동 처리되지만, 안 되면 `키 보내기` `{Enter}`

### 단계 7 — 성공 확인 & 기록
1. 상신 후 목록 화면에서 `웹 페이지에서 세부 정보 가져오기`로 오늘 제목이 존재하는지 확인
2. `텍스트를 파일에 쓰기`: `C:\PTW\log.txt`에 `%작업일자% 상신 성공/실패` 기록
3. 실패 시 `스크린샷 찍기`로 화면 저장 → 원인 확인용

### 오류 처리 (중요)
- 흐름 전체를 `블록 오류 시` 로 감싸고: 실패하면 스크린샷 저장 + 로그 기록 후 종료
- 무인 실행이므로 **실패해도 조용히 죽지 않고 흔적을 남기는 것**이 핵심

---

## 2. 매일 자동 실행 (무인화)

PAD 무료 버전은 자체 예약 실행이 없으므로 **Windows 작업 스케줄러** 사용:

1. 작업 스케줄러 → 기본 작업 만들기
2. 트리거: 매일 07:30 (원하는 시각)
3. 동작: 프로그램 시작
   - 프로그램: `"C:\Program Files (x86)\Power Automate Desktop\PAD.Console.Host.exe"`
   - 인수: `ms-powerautomate:/console/flow/run?workflowName=COTIS_PTW_상신`
   - (PAD 버전에 따라 URL 방식이 다를 수 있음 — PAD 흐름 목록에서 흐름 우클릭 → 속성에 실행 URL 표시됨)
4. 조건: "AC 전원일 때만" 해제, PC 자동 켜짐/절전 해제 설정
5. **PC가 로그인된 상태여야 UI 자동화가 동작** → 자동 로그인 설정 또는 화면잠금 해제 상태 유지 필요 (보안 규정 확인)

---

## 3. 매일 바뀌는 내용 관리 (선택)

작업내용이 날마다 다르면, 엑셀 한 장으로 관리:

- `C:\PTW\작업계획.xlsx` : A열=날짜, B열=작업내용, C열=위치, D열=인원 …
- PAD 액션 `Excel 시작` → `Excel 워크시트에서 읽기`로 오늘 날짜 행을 찾아 변수에 담아 단계 4에 입력
- 전날 퇴근 전에 엑셀만 한 줄 채워두면, 다음날 아침 자동 상신

---

## 4. 구축 순서 (권장)

1. **1주차**: 로그인 → 기안화면 이동까지만 만들어 안정 동작 확인
2. **2주차**: 입력·첨부·상신까지 완성, 단 마지막 상신 버튼은 **수동으로 눌러** 검증
3. **3주차**: 상신까지 자동화 + 스케줄러 등록, 며칠간 결과 모니터링
4. 안정화 후 완전 무인 전환

---

## 5. 주의사항

- **회사/LH 보안규정** 상 자동화 도구 사용 가능 여부 사전 확인
- 비밀번호를 흐름·파일에 평문 저장 금지 (Windows 자격 증명 관리자 사용)
- COTIS 화면 개편 시 UI 요소를 다시 지정해야 함 (PAD에서 해당 액션의 UI 요소만 재선택하면 됨)
- 상신 = 공식 결재행위. 자동 상신 전 내용 검증 로직(엑셀 값 비어있으면 중단 등)을 꼭 넣을 것

---

## 6. 다음 단계

COTIS 화면 캡처(로그인 화면, 메뉴 경로, 기안 작성 화면)를 확보하면
위 단계 2~6의 UI 요소를 실제 화면 기준으로 구체화한 상세 절차서를 만들 수 있음.
